# دليل نشر وتشغيل نظام مستشفى الوفاء على استضافة مجانية 🚀

هذا الدليل يوضح لك بالخطوات البسيطة كيفية رفع المشروع على استضافة مجانية لتشغيله عبر **رابط مباشر (URL)** يمكنك مشاركته مع أي شخص لفتحه من الهاتف أو اللابتوب مباشرة دون الحاجة لتشغيل أي سيرفر محلي.

---

## 🌟 الخيار الأول: النشر الفوري بنقرة واحدة (أسرع وأسهل خيار — دقيقة واحدة فقط)

الواجهة الأمامية (`wafa-web`) مبنية لتعمل بنظام تخزين وسياق سريري مستقل (`HospitalContext` + `localStorage`). جميع الشاشات (العيادات، الأسنان، العلاج الطبيعي، كبار السن، المبيت، الطابور، الكاشير، الأشعة، المختبر) تعمل فوراً وبشكل تفاعلي 100%.

### الخطوات على Netlify (مجاني 100% بدون بطاقة بنكية):
1. افتح الرابط التالي في المتصفح:
   👉 **[https://app.netlify.com/drop](https://app.netlify.com/drop)**
2. قم بسحب المجلد التالي من جهازك وأفلته في الصفحة:
   📁 `c:\xampp\htdocs\wafa-hospital-system\wafa-web\dist`
3. سيكتمل الرفع في ثوانٍ وستحصل فوراً على رابط مباشر مثل:
   🔗 `https://wafaa-hospital-system.netlify.app`
4. شارك الرابط وافتحه من أي جهاز لتجربة النظام بالكامل!

---

## 🚀 الخيار الثاني: رفع المشروع كـ Full-Stack كامل (React + Laravel + Database)

إذا أردت رفع الواجهة الخلفية (Laravel API) مع قاعدة بيانات سحابية متصلة بالكامل:

### أ) عبر منصة [Render.com](https://render.com) (مجانية 100%):
1. ارفع كود المشروع إلى مستودع خاص بك على **GitHub**.
2. ادخل على **Render.com** وأنشئ حساباً مجانياً.
3. **إنشاء خدمة الباك إند (Laravel API):**
   - اختر **New Web Service** واربط مستودع الـ GitHub.
   - حدد مجلد العمل (Root Directory): `wafa-api`.
   - اختر البيئة **Docker** (تم تجهيز ملف `Dockerfile` مسبقاً).
   - في المتغيرات البيئية (Environment Variables) أضف:
     - `APP_KEY` = `base64:yNpu9seBu/PvL+ttXpM4ijYVvig32FV8vpZipOr8Z5E=`
     - `APP_ENV` = `production`
     - `DB_CONNECTION` = `sqlite` (لتشغيل قاعدة بيانات سحابية مدمجة فورية)
   - سيعطيك Render رابطاً للباك مثل: `https://wafa-api.onrender.com`.

4. **نشر الفرونت إند (React):**
   - اختر **New Static Site** على Render (أو Vercel).
   - حدد مجلد العمل: `wafa-web`.
   - أمر البناء (Build Command): `npm run build`.
   - مجلد النشر (Publish Directory): `dist`.
   - أضف المتغير البيئي:
     - `VITE_API_BASE_URL` = `https://wafa-api.onrender.com/api/v1`

---

## 🔐 حسابات الدخول المعتمدة لتجربة النظام

| الدور / الحساب | اسم المستخدم (Username) | كلمة المرور (Password) |
|---|---|---|
| **الكاشير والاستقبال** | `cashier` | `Cashier@2026` |
| **أمين الصندوق (المالية)** | `Eng.Mohammed_Moqbil` | `Treasury@2026` |
| **رئيس الـ IT (الإدارة)** | `Eng.Ahmed_Jaber` | `IT@2026` |
| **إدارة المستشفى (عرض فقط)** | `Dr.Fouad_Najm` | `Admin@2026` |
| **طبيب العيادة** | يتم إنشاؤه عبر شاشة الأطباء | كلمة المرور المحددة |
