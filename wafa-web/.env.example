# Leave empty during local development; Vite proxies /api to Laravel on port 18088.
VITE_API_BASE_URL=

# Civil Registry credentials must never be placed in this frontend file.
# Configure them only in wafa-api/.env as documented in docs/CIVIL_REGISTRY_API_SETUP.md.
